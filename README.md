# 초기 파이썬 설치 방법
    cmd 관리자 권한으로 실행
    winget p\install python.python.3.11
    이용약관 동의 y 엔터
    python --version 3.11.x 나오면 성공!
    py -m install pygame
    
# 그다음에 
    add commit push
# 내가 지금 작업 하는 폴더가 초신이 아니라면
    git pull origin master

# 현재 상태를 확인하기 위해서는
    git status

# 마크다운 md를 마크다운 에디터에서 사용.
    markdown editor를 가지고 design.md를 가지고 사용가능
